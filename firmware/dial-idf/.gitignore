# Secrets (Wi-Fi creds) — optional dev seed, copy secrets.h.example to secrets.h
main/secrets.h

# ESP-IDF build output + fetched dependencies (regenerated by idf.py)
build/
managed_components/
sdkconfig
sdkconfig.old
.cache/
